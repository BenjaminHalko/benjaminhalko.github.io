---
import { ClientRouter, fade } from "astro:transitions";
import Navbar from "../components/Navbar.astro";
import "../styles/main.css";

interface OpenGraph {
  readonly url?: string;
  readonly title?: string;
  readonly description?: string;
  readonly image?: string;
  readonly imageType?: string;
  readonly type?: string;
}

interface TwitterCard {
  readonly card?: string;
  readonly url?: string;
  readonly title?: string;
  readonly description?: string;
  readonly image?: string;
}

export interface Props {
  readonly title: string;
  readonly description?: string;
  readonly robots?: string;
  readonly og?: OpenGraph;
  readonly twitter?: TwitterCard;
}

const { title, description, robots, og, twitter } = Astro.props;
---

<!doctype html>
<html lang="en" transition:animate={fade({ duration: "320ms" })}>
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <!-- A meta tag, not CSS: applies before any stylesheet loads. -->
    <meta name="color-scheme" content="dark" />
    <slot name="favicons" />
    {robots && <meta name="robots" content={robots} />}
    <title>{title}</title>
    {description && <meta name="description" content={description} />}
    <ClientRouter />

    {og?.type && <meta property="og:type" content={og.type} />}
    {og?.url && <meta property="og:url" content={og.url} />}
    {og?.title && <meta property="og:title" content={og.title} />}
    {
      og?.description && (
        <meta property="og:description" content={og.description} />
      )
    }
    {og?.image && <meta property="og:image" content={og.image} />}
    {
      og?.imageType && (
        <meta property="og:image:type" content={og.imageType} />
      )
    }

    {
      twitter?.card && <meta name="twitter:card" content={twitter.card} />
    }
    {twitter?.url && <meta name="twitter:url" content={twitter.url} />}
    {
      twitter?.title && <meta name="twitter:title" content={twitter.title} />
    }
    {
      twitter?.description && (
        <meta name="twitter:description" content={twitter.description} />
      )
    }
    {
      twitter?.image && <meta name="twitter:image" content={twitter.image} />
    }
    <slot name="head" />
  </head>
  <body>
    <Navbar />
    <slot />
    <script>
      // transition:persist keeps the navbar node, but re-inserting it into the
      // new document restarts CSS animations. Mark incoming documents so
      // navbar.css suppresses the drop-in on every navigation after the first
      // load. Registered once; ClientRouter keeps module scripts alive.
      interface BeforeSwapEvent extends Event {
        readonly newDocument: Document;
      }

      document.addEventListener("astro:before-swap", (event) => {
        const e = event as BeforeSwapEvent;
        e.newDocument.documentElement.dataset.navSettled = "";
      });
    </script>
  </body>
</html>
